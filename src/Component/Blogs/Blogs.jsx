import {Helmet} from "react-helmet";

const Blogs = () => {
    return (
        <div>
            <Helmet>
                <title>Blogs</title>
            </Helmet>
        </div>
    );
};

export default Blogs;