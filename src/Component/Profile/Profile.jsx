import {Helmet} from "react-helmet";

const Profile = () => {
    return (
        <div>
            <Helmet>
                <title>update-profile</title>
            </Helmet>
            its update
        </div>
    );
};

export default Profile;